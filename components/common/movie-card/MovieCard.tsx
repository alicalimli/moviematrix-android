import React from "react";
import { Text } from "react-native";

interface MovieCardProps {}

const MovieCard = ({}: MovieCardProps) => {
  return <Text>MovieCard</Text>;
};

export default MovieCard;
